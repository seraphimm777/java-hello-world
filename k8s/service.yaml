apiVersion: v1
kind: Service
metadata:
  name: java-hello-world-svc
spec:
  type: NodePort
  selector:
    app: java-hello-world
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30036
